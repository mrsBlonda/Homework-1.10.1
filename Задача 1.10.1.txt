В метаспейс загружается класс JvmComprehension
Потом данный класс загружается в ClassLoader
После того как прошла инициилизация (код прошел поверку на валидность, прошла стадия подготовки стат. полей, методов, и стадия проверки символьных методов) начинает выполняться метод main
В стэке создается переменная int = 1
В куче создается объект Object, а в стэке создается переменная o, которая ссылается на Object 
В куче создается объект Integer, а в стэке создается переменная ii, которая ссылается на Integer
В стэке фрэйм создается  printAll
В нем создается переменная o, которая также ссылается на тот же объект в куче Object
Создается переменная i со значением, которое мы ему присвоим, или же это будет 1 или любое другое
Создается переменная ii, которая ссылается на Integer в куче
В куче создается объект Integer (другой), а в стэке создается переменная uselessVar , которая ссылается на Integer
В стэке создается фрэйм println
В стэке создается фрейм toString и в нем создается переменная o, которая ссылается на объет Object
Во фрейме println создаются перемнные i и ii, аналогично 
конец фрейма printAll
В стэке создается фрэйм println
В нем создается переменная (вот под каким названием создается не знаю), которая ссылается на объект String в куче

Сборщик мусора удалит объект Integer, на который ссылается переменная uselessVar
